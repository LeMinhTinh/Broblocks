#pragma once

#include "iostream"
#include "vector"
#include "set"
using namespace std;


class STLVector
{
private:

public:
	bool hasSpaces(string str);
	string removeSpace(string str);
	void removeduplicate ( vector<int> arr);
	void dupLastIndex(vector<int> arr);


};